apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: skypilot-secrets
  namespace: skypilot
spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  refreshPolicy: Periodic
  refreshInterval: "1h"
  target:
    name: skypilot-secrets
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      type: Opaque
      engineVersion: v2
      data:
        client-id: "{{ .CLIENT_ID }}"
        client-secret: "{{ .CLIENT_SECRET }}"
  data:
    - secretKey: CLIENT_ID
      remoteRef:
        key: skypilot
        property: CLIENT_ID
    - secretKey: CLIENT_SECRET
      remoteRef:
        key: skypilot
        property: CLIENT_SECRET
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: skypilot-postgres-secrets
  namespace: skypilot
spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  refreshPolicy: Periodic
  refreshInterval: "1h"
  target:
    name: skypilot-postgres-secrets
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      type: Opaque
      engineVersion: v2
      data:
        connection_string: postgresql://skypilot:{{ .POSTGRES_PASSWORD }}@pooler-rw.postgres.svc.cluster.local:5432/skypilot
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: skypilot
        property: POSTGRES_PASSWORD
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vastai-secrets
  namespace: skypilot
spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  refreshPolicy: Periodic
  refreshInterval: "1h"
  target:
    name: vastai-secrets
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      type: Opaque
      engineVersion: v2
      data:
        api_key: "{{ .vastai_api_key }}"
  data:
    - secretKey: vastai_api_key
      remoteRef:
        key: skypilot
        property: VASTAI_API_KEY
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: runpod-secrets
  namespace: skypilot
spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  refreshPolicy: Periodic
  refreshInterval: "1h"
  target:
    name: runpod-secrets
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      type: Opaque
      engineVersion: v2
      data:
        api_key: "{{ .runpod_api_key }}"
  data:
    - secretKey: runpod_api_key
      remoteRef:
        key: skypilot
        property: RUNPOD_API_KEY
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: r2-secrets
  namespace: skypilot
spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  refreshPolicy: Periodic
  refreshInterval: "1h"
  target:
    name: r2-secrets
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      type: Opaque
      engineVersion: v2
      data:
        "r2.credentials": |
          [r2]
          aws_access_key_id={{ .r2_access_key }}
          aws_secret_access_key={{ .r2_secret_key }}
        accountid: "{{ .r2_account_id }}"
  data:
    - secretKey: r2_access_key
      remoteRef:
        key: skypilot
        property: R2_ACCESS_KEY
    - secretKey: r2_secret_key
      remoteRef:
        key: skypilot
        property: R2_SECRET_KEY
    - secretKey: r2_account_id
      remoteRef:
        key: skypilot
        property: R2_ACCOUNT_ID
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: skypilot
  namespace: skypilot
spec:
  interval: 1h
  timeout: 10m
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: skypilot-nightly
      version: "1.0.0-dev.20260319"
      sourceRef:
        kind: HelmRepository
        name: skypilot
        namespace: flux-system
  values:
    rbac:
      manageSystemComponents: false
    apiService:
      dbConnectionSecretName: skypilot-postgres-secrets
      skipResourceCheck: true
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
        limits:
          cpu: 4000m
          memory: 4096Mi

    auth:
      oauth:
        enabled: true
        oidc-issuer-url: https://id.nokiy.net
        client-details-from-secret: "skypilot-secrets"
        session-store-type: redis
        redis-url: redis://valkey-skypilot:6379
    storage:
      enabled: true
      storageClassName: local-path
      accessMode: ReadWriteOnce
      size: 10Gi
    ingress:
      enabled: true
      host: sky.nokiy.net
      ingressClassName: traefik
      annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
        traefik.ingress.kubernetes.io/router.tls: "true"
        cert-manager.io/cluster-issuer: letsencrypt-prod
        external-dns.alpha.kubernetes.io/hostname: sky.nokiy.net
        external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
      tls:
        enabled: true
    ingress-nginx:
      enabled: false
    awsCredentials:
      enabled: false
    kubernetesCredentials:
      useApiServerCluster: true
    vastCredentials:
      enabled: true
      vastSecretName: vastai-secrets
    runpodCredentials:
      enabled: true
      runpodSecretName: runpod-secrets
    r2Credentials:
      enabled: true
      r2SecretName: r2-secrets
