apiVersion: v1
kind: Service
metadata:
  name: usesend
  namespace: usesend
  labels:
    app.kubernetes.io/name: usesend
    app.kubernetes.io/instance: usesend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: http
    name: http
  selector:
    app.kubernetes.io/name: usesend
    app.kubernetes.io/instance: usesend
