---
apiVersion: v1
kind: Service
metadata:
  name: real-browser
  namespace: rsshub
  labels:
    app.kubernetes.io/component: real-browser
    app.kubernetes.io/part-of: rsshub
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rsshub
    app.kubernetes.io/instance: rsshub
    app.kubernetes.io/component: real-browser
    app.kubernetes.io/part-of: rsshub
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
