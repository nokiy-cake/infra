apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mlflow
  namespace: mlflow
spec:
  interval: 1h
  chart:
    spec:
      chart: mlflow
      version: "1.8.1"
      sourceRef:
        kind: HelmRepository
        name: community-charts
        namespace: flux-system
