apiVersion: v1
kind: ConfigMap
metadata:
  name: atuin-server-config
  namespace: atuin
data:
  server.toml: |
    host = "0.0.0.0"
    port = 8888
    open_registration = true
    
    [tls]
    enable = true
    cert_path = "/etc/atuin/tls/tls.crt"
    pkey_path = "/etc/atuin/tls/tls.key"
