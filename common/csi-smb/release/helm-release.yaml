apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-smb
  namespace: smb-csi
spec:
  interval: 5m
  chart:
    spec:
      chart: csi-driver-smb
      version: 'v1.19.1'
      sourceRef:
        kind: HelmRepository
        name: csi-driver-smb
        namespace: flux-system
