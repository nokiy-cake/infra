apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vmo
  namespace: vm
spec:
  interval: 1h
  chart:
    spec:
      chart: victoria-metrics-operator
      version: '0.58.1'
      sourceRef:
        kind: HelmRepository
        name: vm
        namespace: flux-system
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
