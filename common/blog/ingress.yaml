apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blog
  namespace: ni-svc
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/hostname: nibapi.nokiy.net
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  tls:
    - hosts:
        - nibapi.nokiy.net
      secretName: blog-tls
  rules:
    - host: nibapi.nokiy.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: blog
                port:
                  number: 80
