apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omnigent-artifacts
  namespace: omnigent
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
