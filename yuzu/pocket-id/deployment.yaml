apiVersion: apps/v1
kind: Deployment
metadata:
  name: pocket-id
  namespace: pocket-id
  labels:
    app.kubernetes.io/name: pocket-id
    app.kubernetes.io/instance: pocket-id
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: pocket-id
      app.kubernetes.io/instance: pocket-id
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pocket-id
        app.kubernetes.io/instance: pocket-id
    spec:
      serviceAccountName: pocket-id
      initContainers:
        - name: litestream
          image: litestream/litestream:0.5.0
          imagePullPolicy: IfNotPresent
          restartPolicy: Always
          args:
            - replicate
          volumeMounts:
            - name: data
              mountPath: /data
            - name: litestream-config
              mountPath: /etc
              subPath: litestream.yml
              readOnly: true
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 128Mi
      containers:
        - name: pocket-id
          image: ghcr.io/pocket-id/pocket-id:v1.13.0-distroless
          imagePullPolicy: IfNotPresent
          ports:
            - name: pocket-id-port
              containerPort: 1411
              protocol: TCP
          env:
            - name: APP_NAME
              value: "NOKIY ID"
            - name: APP_URL
              value: "https://id.nokiy.net"
            - name: TRUST_PROXY
              value: "trust"
            - name: ENCRYPTION_KEY 
              valueFrom:
                secretKeyRef:
                  name: pocket-id-secrets
                  key: ENCRYPTION_KEY
            - name: ANALYTICS_DISABLED 
              value: "true"
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
          volumeMounts:
            - name: data
              mountPath: /app/data
          livenessProbe:
            exec:
              command:
                - /app/pocket-id
                - healthcheck
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - /app/pocket-id
                - healthcheck
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pocket-id-data
        - name: litestream-config
          secret:
            secretName: litestream-secrets
