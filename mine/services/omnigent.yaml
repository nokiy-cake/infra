apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: omnigent
  namespace: flux-system
spec:
  # Retire the legacy Traefik/Cloudflare deployment without pruning its data
  # while the production Talos Flux stack owns this hostname.
  suspend: true
  interval: 5m
  path: ./common/omnigent
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  timeout: 10m
  wait: true
  dependsOn:
    - name: traefik
    - name: external-secrets-config
    - name: cert-manager-letsencrypt
    - name: external-dns-release
    - name: cnpg
    - name: pocket-id
